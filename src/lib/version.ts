/**
 * App version. Bump APP_VERSION on each release and give it a short codename.
 * Shown in Settings and the update prompt so it's clear which build is running.
 */
export const APP_VERSION = '1.11.0'
export const APP_VERSION_NAME = 'Spare Change'

export const versionLabel = `v${APP_VERSION}${APP_VERSION_NAME ? ` · ${APP_VERSION_NAME}` : ''}`
